# --- PORT ---
PORT=8510

# --- Timezone ---
TZ=America/New_York

# --- Languages ---
UI_LANGUAGE=en
SUBTITLES_CHOICES=en

# Update .env environment variables depending of your Docker or local run setup.

# --- Docker ---
## --- Docker host paths ---
# Docker host environment variables to specify depending on your homelab setup.
VIDEOS_FOLDER_DOCKER_HOST=/mnt/data/videos
TMP_DOWNLOAD_FOLDER_DOCKER_HOST=/mnt/data/hometube/tmp
YOUTUBE_COOKIES_FILE_PATH_DOCKER_HOST=/opt/cookies/youtube.txt

## --- Docker container paths ---
# Internal Docker container paths (DO NOT CHANGE unless you know what you are doing).
VIDEOS_FOLDER=/data/videos
TMP_DOWNLOAD_FOLDER=/data/tmp
YOUTUBE_COOKIES_FILE_PATH=/config/youtube_cookies.txt


# --- Local run ---
VIDEOS_FOLDER=./downloads
TMP_DOWNLOAD_FOLDER=./tmp
#YOUTUBE_COOKIES_FILE_PATH=./cookies/youtube_cookies.txt
COOKIES_FROM_BROWSER=chrome


# --- Quality & Download Preferences ---
# Default download mode: "auto" (try all profiles) or "forced" (single profile only)
#DOWNLOAD_MODE=auto

# Maximum video resolution: "max" for highest available, or specific resolution
# Options: max, 2160, 1440, 1080, 720, 480, 360
#VIDEO_QUALITY_MAX=max

# Refuse quality downgrade: "true" to stop at first failure, "false" for fallback
#REFUSE_QUALITY_DOWNGRADE=false

# Embed chapters and subtitles by default
#EMBED_CHAPTERS=true
#EMBED_SUBTITLES=true

# --- yt-dlp ---
# Custom yt-dlp arguments (example: --proxy http://proxy:8080 --max-filesize 100M)
#YTDLP_CUSTOM_ARGS=--max-filesize 100M --write-info-json

# --- Browser Configuration ---
# Default browser for cookie extraction
#BROWSER_SELECT=chrome
